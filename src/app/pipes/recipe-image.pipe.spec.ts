import { RecipeImagePipe } from './recipe-image.pipe';

describe('RecipeImagePipe', () => {
  it('create an instance', () => {
    const pipe = new RecipeImagePipe();
    expect(pipe).toBeTruthy();
  });
});
